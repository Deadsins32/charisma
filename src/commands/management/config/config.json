{
    "permissions": ["all"],
    "description": "gets or sets a specific configuration value",
    "hidden": false,
    "nsfw": false,
    "tags": ["management", "utility"],
    "params": [
        { "type": "string", "required": true, "name": "get | tree | set" },
        { "type": "string", "required": false, "name": "value" }
    ]
}